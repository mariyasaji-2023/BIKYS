<%- include('partials/header.ejs') %>

<!-- <!DOCTYPE HTML>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Home World</title>
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta property="og:title" content="">
    <meta property="og:type" content="">
    <meta property="og:url" content="">
    <meta property="og:image" content=""> -->
    
    <section class="content-main mt-20 mb-20">
        <div class="card mx-auto card-login">
            <div class="card-body">
                <h4 class="card-title mb-4">Email verification with OTP</h4>
    
                <form action="/postotp" method="post">
                    <p class="text-success" id="errorShow">
                        <%= err %>
                    </p>
                    <div class="mb-3" id="otpField">
                        <span class="mobile-text mb-2">Enter the code that we just send on your email <b class="text-danger"></b></span>
                        <input class="form-control mt-20" type="text" id='otp' name="otp" placeholder="Enter OTP">
                    </div>
                    <div id="otp-timer">
                        <div id="timer">
                            03:00
                        </div>
                    </div>
                    <div class="mb-4" id="loginBtn">
                        <button type="submit" class="btn btn-primary w-100" onclick="showAlert()">Verify OTP</button>
                    </div>
                    
    
                    <div class="text-center mt-5">
                        <span class="d-block mobile-text">Don't receive the code?</span>
                        <!-- Add an ID to the button to show/hide it -->
                        <button class="button btn-primary text-light cursor" type="button" id="resendOtp" onclick="resendOtp()" style="display: none;">Resend</button>
                    </div>
                    
                </form>
            </div>
        </div>
    </section>
        
        <footer class="main-footer text-center">
            <p class="font-xs">
                <script>
                    document.write(new Date().getFullYear())
                </script> ©BIKYZ.
            </p>
            <p class="font-xs pb-20">All rights reserved</p>
        </footer>
    </main>
   
    <!-- Your existing script code -->
    <script>
        // Function to handle OTP resend
        function resendOtp() {
            // Perform an AJAX request to resend the OTP
            fetch('/resend-otp', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({}),
            })
                .then(response => response.json())
                .then(data => {
                    // Handle the response
                    if (data.status === true) {
                        console.log('OTP resent successfully');
                        // You can add additional logic here, such as updating UI elements
                    } else {
                        console.error('Error resending OTP');
                        // Handle the error, display an alert, or update UI accordingly
                    }
                })
                .catch(error => {
                    console.error('Error in fetch:', error);
                });
        }
    
        // Function to show the "Resend OTP" button after 3 minutes
        function showResendButton() {
            document.getElementById('resendOtp').style.display = 'block';
        }
    
        // otp
        var timer;
        var Otptimer = document.getElementById('timer');
        var resendTimer = document.getElementById('resendOtp');
    
        function Otpstarttimer(minute, sec) {
            clearInterval(timer)
            timer = setInterval(() => {
                Otptimer.innerHTML = formatTime(minute) + ':' + formatTime(sec);
                if (minute == 0 && sec == 0) {
                    clearInterval(timer);
                    // Call the function to show the "Resend OTP" button
                    showResendButton();
                } else {
                    if (sec == 0) {
                        minute--;
                        sec = 59;
                    } else {
                        sec--;
                    }
                }
            }, 1000)
        }
    
        function formatTime(time) {
            return time < 10 ? '0' + time : time;
        }
        Otpstarttimer(1, 0);
    
        resendTimer.addEventListener('click', function () {
            Otpstarttimer(1, 0);
        });
    </script>

       
    <script>
        document.getElementById('resendOtp').addEventListener('click', function () {
            resendOtp();
        });
    </script>
    <!-- <script>
        function showAlert() {
            // Display an alert when the button is clicked
            alert('User registered successfully');
        }
    </script> -->

    <script src="/assets/js/vendors/jquery-3.6.0.min.js"></script>
    <script src="/assets/js/vendors/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/vendors/jquery.fullscreen.min.js"></script>
    <!-- Main Script -->
    <script src="/assets/js/main.js" type="text/javascript"></script>
</body>

<%- include('partials/footer.ejs') %>